{
  "status": true,
  "data": {
    "{{#each data}}": {
      "id": "{{#? this.id}}",
      "name": "{{#? this.name}}",
      "unit": "{{#? this.Unit}}",
      "methodology": "{{#? this.Method}}",
      "examGroup": "{{#? this.ExaminationGroup}}",
      "examinationReferenceValues": {
        "{{#each this.examinationReferenceValues}}": {
          "id": "{{#? this.id}}",
          "name": "{{#? this.name}}"
        }
      }
    }
  }
}
