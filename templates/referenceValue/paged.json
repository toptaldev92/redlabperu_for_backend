{
  "status": true,
  "total": "{{#? data.total}}",
  "count": "{{#? data.count}}",
  "data": {
    "{{#each data.rows}}": {
      "id": "{{#? this.id}}",
      "name": "{{#? this.name}}",
      "examinationReferenceValues": "{{#? this.examinationReferenceValues}}"
    }
  }
}
